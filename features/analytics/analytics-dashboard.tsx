import { Card } from "@/components/ui/card";
import { formatPHP } from "@/lib/currency";

type AnalyticsData = Awaited<ReturnType<typeof import("@/lib/server-data").getAnalyticsData>>;

export function AnalyticsDashboard({ data }: { data: AnalyticsData }) {
  const maxCategory = data.spendingByCategory[0]?.total ?? 1;
  return <div className="space-y-6"><div><p className="text-sm font-medium text-blue-600 dark:text-blue-300">Your patterns</p><h1 className="mt-1 text-3xl font-semibold tracking-tight text-slate-900 dark:text-white">Analytics</h1><p className="mt-1 text-sm text-slate-600 dark:text-slate-300">A focused view of your academic, productivity, and financial momentum.</p></div><div className="grid gap-6 xl:grid-cols-2"><Card className="rounded-[24px] p-5 dark:bg-slate-900"><p className="text-xs font-semibold uppercase tracking-[0.16em] text-slate-500">Academic</p><div className="mt-5 grid grid-cols-2 gap-4"><div><p className="text-sm text-slate-500">Completion rate</p><p className="mt-2 text-3xl font-semibold text-blue-600">{data.completionRate}%</p></div><div><p className="text-sm text-slate-500">Total assignments</p><p className="mt-2 text-3xl font-semibold text-slate-900 dark:text-white">{data.totalAssignments}</p></div><div><p className="text-sm text-slate-500">Completed</p><p className="mt-2 text-xl font-semibold text-emerald-600">{data.completedAssignments}</p></div><div><p className="text-sm text-slate-500">Pending</p><p className="mt-2 text-xl font-semibold text-amber-600">{data.pendingAssignments}</p></div></div><div className="mt-5 rounded-2xl bg-slate-50 p-4 dark:bg-slate-950"><p className="text-xs text-slate-500">Most demanding course</p><p className="mt-1 font-semibold text-slate-900 dark:text-white">{data.mostDemandingCourse ? `${data.mostDemandingCourse.code} · ${data.mostDemandingCourse.title}` : "Not enough data yet"}</p></div></Card><Card className="rounded-[24px] p-5 dark:bg-slate-900"><p className="text-xs font-semibold uppercase tracking-[0.16em] text-slate-500">Productivity</p><div className="mt-5 grid grid-cols-2 gap-4"><div><p className="text-sm text-slate-500">Notes this month</p><p className="mt-2 text-3xl font-semibold text-slate-900 dark:text-white">{data.notesThisMonth}</p></div><div><p className="text-sm text-slate-500">Study sessions</p><p className="mt-2 text-3xl font-semibold text-emerald-600">{data.studySessions}</p></div></div><div className="mt-6"><div className="flex items-center justify-between text-xs text-slate-500"><span>Weekly activity</span><span>{data.weeklyAssignments} updated assignments</span></div><div className="mt-3 h-3 rounded-full bg-slate-100 dark:bg-slate-800"><div className="h-3 rounded-full bg-blue-600" style={{ width: `${Math.min(100, data.weeklyAssignments * 12.5)}%` }} /></div></div></Card><Card className="rounded-[24px] p-5 dark:bg-slate-900 xl:col-span-2"><p className="text-xs font-semibold uppercase tracking-[0.16em] text-slate-500">Finance</p><div className="mt-5 grid gap-4 sm:grid-cols-4"><div><p className="text-sm text-slate-500">Monthly income</p><p className="mt-2 text-2xl font-semibold text-emerald-600">{formatPHP(data.income)}</p></div><div><p className="text-sm text-slate-500">Monthly expense</p><p className="mt-2 text-2xl font-semibold text-slate-900 dark:text-white">{formatPHP(data.spending)}</p></div><div><p className="text-sm text-slate-500">Savings</p><p className="mt-2 text-2xl font-semibold text-blue-600">{formatPHP(data.savings)}</p></div><div><p className="text-sm text-slate-500">Top category</p><p className="mt-2 truncate text-lg font-semibold text-slate-900 dark:text-white">{data.topSpendingCategory?.name ?? "None yet"}</p></div></div><div className="mt-6 space-y-4">{data.spendingByCategory.length ? data.spendingByCategory.map((category) => <div key={category.name}><div className="flex justify-between text-xs text-slate-500"><span>{category.name}</span><span>{formatPHP(category.total)}</span></div><div className="mt-2 h-3 rounded-full bg-slate-100 dark:bg-slate-800"><div className="h-3 rounded-full bg-blue-600" style={{ width: `${Math.max(4, (category.total / maxCategory) * 100)}%` }} /></div></div>) : <p className="text-sm text-slate-500">No spending data for this month.</p>}</div></Card></div></div>;
}