"use client";

import { useState } from "react";
import { Download, MoonStar, Save, SunMedium, Trash2 } from "lucide-react";
import { useTheme } from "next-themes";
import { signOut } from "next-auth/react";

import { Button } from "@/components/ui/button";
import { Card } from "@/components/ui/card";

export function SettingsPage({ user }: { user: { name: string; email: string } }) {
  const { resolvedTheme, setTheme } = useTheme();
  const [name, setName] = useState(user.name);
  const [currentPassword, setCurrentPassword] = useState("");
  const [newPassword, setNewPassword] = useState("");
  const [message, setMessage] = useState("");
  const [error, setError] = useState("");
  const [confirmation, setConfirmation] = useState("");
  const [deleting, setDeleting] = useState(false);

  const updateProfile = async (event: React.FormEvent) => { event.preventDefault(); setError(""); setMessage(""); const response = await fetch("/api/settings/profile", { method: "PATCH", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ name }) }); const payload = await response.json(); if (!response.ok) { setError(payload.error ?? "Unable to update profile."); return; } setMessage("Profile updated."); };
  const updatePassword = async (event: React.FormEvent) => { event.preventDefault(); setError(""); setMessage(""); const response = await fetch("/api/settings/profile", { method: "PATCH", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ currentPassword, newPassword }) }); const payload = await response.json(); if (!response.ok) { setError(payload.error ?? "Unable to update password."); return; } setCurrentPassword(""); setNewPassword(""); setMessage("Password updated."); };
  const exportData = async () => { const response = await fetch("/api/settings/profile"); if (!response.ok) { setError("Unable to export your data."); return; } const blob = new Blob([JSON.stringify(await response.json(), null, 2)], { type: "application/json" }); const url = URL.createObjectURL(blob); const link = document.createElement("a"); link.href = url; link.download = "student-os-export.json"; link.click(); URL.revokeObjectURL(url); setMessage("Your data export has been downloaded."); };
  const deleteAccount = async () => { if (confirmation !== "DELETE") return; setDeleting(true); const response = await fetch("/api/settings/account", { method: "DELETE", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ confirmation }) }); if (!response.ok) { setError("Unable to delete account."); setDeleting(false); return; } await signOut({ callbackUrl: "/login" }); };

  return <div className="mx-auto max-w-4xl space-y-6"><div><p className="text-sm font-medium text-blue-600 dark:text-blue-300">Workspace preferences</p><h1 className="mt-1 text-3xl font-semibold tracking-tight text-slate-900 dark:text-white">Settings</h1><p className="mt-1 text-sm text-slate-600 dark:text-slate-300">Keep your profile, appearance, and data under your control.</p></div>{message ? <div role="status" aria-live="polite" className="rounded-xl border border-emerald-200 bg-emerald-50 px-4 py-3 text-sm text-emerald-700 dark:border-emerald-500/30 dark:bg-emerald-500/10 dark:text-emerald-200">{message}</div> : null}{error ? <div role="alert" className="rounded-xl border border-rose-200 bg-rose-50 px-4 py-3 text-sm text-rose-700 dark:border-rose-500/30 dark:bg-rose-500/10 dark:text-rose-200">{error}</div> : null}<Card className="rounded-[24px] p-5 dark:bg-slate-900 sm:p-6"><p className="text-xs font-semibold uppercase tracking-[0.16em] text-slate-500">Profile</p><form className="mt-5 space-y-4" onSubmit={updateProfile}><label className="block text-sm font-medium text-slate-700 dark:text-slate-200">Display name<input value={name} onChange={(event) => setName(event.target.value)} className="mt-2 h-11 w-full rounded-xl border border-slate-200 bg-white px-3 text-sm dark:border-slate-700 dark:bg-slate-950" /></label><label className="block text-sm font-medium text-slate-700 dark:text-slate-200">Email<input value={user.email} readOnly className="mt-2 h-11 w-full rounded-xl border border-slate-200 bg-slate-50 px-3 text-sm text-slate-500 dark:border-slate-700 dark:bg-slate-950" /></label><Button type="submit"><Save className="h-4 w-4" />Save profile</Button></form></Card><Card className="rounded-[24px] p-5 dark:bg-slate-900 sm:p-6"><p className="text-xs font-semibold uppercase tracking-[0.16em] text-slate-500">Theme</p><div className="mt-5 flex flex-wrap gap-3"><Button type="button" variant={resolvedTheme === "light" ? "default" : "secondary"} onClick={() => setTheme("light")}><SunMedium className="h-4 w-4" />Light</Button><Button type="button" variant={resolvedTheme === "dark" ? "default" : "secondary"} onClick={() => setTheme("dark")}><MoonStar className="h-4 w-4" />Dark</Button></div></Card><Card className="rounded-[24px] p-5 dark:bg-slate-900 sm:p-6"><p className="text-xs font-semibold uppercase tracking-[0.16em] text-slate-500">Account</p><form className="mt-5 grid gap-4 md:grid-cols-2" onSubmit={updatePassword}><label className="text-sm font-medium text-slate-700 dark:text-slate-200">Current password<input type="password" value={currentPassword} onChange={(event) => setCurrentPassword(event.target.value)} className="mt-2 h-11 w-full rounded-xl border border-slate-200 bg-white px-3 text-sm dark:border-slate-700 dark:bg-slate-950" required /></label><label className="text-sm font-medium text-slate-700 dark:text-slate-200">New password<input type="password" value={newPassword} onChange={(event) => setNewPassword(event.target.value)} className="mt-2 h-11 w-full rounded-xl border border-slate-200 bg-white px-3 text-sm dark:border-slate-700 dark:bg-slate-950" required /></label><div className="md:col-span-2"><Button type="submit">Change password</Button></div></form></Card><Card className="rounded-[24px] p-5 dark:bg-slate-900 sm:p-6"><p className="text-xs font-semibold uppercase tracking-[0.16em] text-slate-500">Data</p><p className="mt-3 text-sm text-slate-600 dark:text-slate-300">Export your profile and Student OS records as a local JSON file.</p><Button type="button" variant="secondary" className="mt-4" onClick={exportData}><Download className="h-4 w-4" />Export JSON</Button><div className="mt-6 border-t border-slate-200 pt-5 dark:border-slate-800"><p className="text-sm font-semibold text-rose-600">Delete account</p><p className="mt-2 text-sm text-slate-500 dark:text-slate-400">This permanently deletes your profile, courses, assignments, notes, schedule, categories, and transactions.</p><div className="mt-3 flex flex-col gap-3 sm:flex-row"><input value={confirmation} onChange={(event) => setConfirmation(event.target.value)} placeholder="Type DELETE" aria-label="Type DELETE to confirm account deletion" className="h-10 rounded-xl border border-rose-200 bg-rose-50 px-3 text-sm dark:border-rose-500/30 dark:bg-rose-500/10" /><Button type="button" variant="danger" disabled={confirmation !== "DELETE" || deleting} onClick={deleteAccount}><Trash2 className="h-4 w-4" />{deleting ? "Deleting..." : "Delete account"}</Button></div></div></Card></div>;
}