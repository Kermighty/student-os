# UI primitives

This folder contains the reusable design-system components used throughout Student OS.

- `button.tsx` provides action states and sizes.
- `card.tsx` defines rounded content surfaces.
- `input.tsx` ensures consistent form styles.
- `badge.tsx` supports chips and status labels.
- `page-header.tsx` standardizes page title sections.
