# Layout components

This folder contains the app shell used by the dashboard and future authenticated screens.

- `app-shell.tsx` provides the responsive main layout.
- `sidebar.tsx` defines the left navigation for desktop screens.
- `top-nav.tsx` manages the sticky global top bar.
- `theme-toggle.tsx` toggles light and dark mode.
