# Components

This directory contains the shared UI and layout building blocks that support the Student OS shell.

- `components/ui` holds reusable primitives such as Button, Card, Input, Badge, and PageHeader.
- `components/layout` contains the responsive sidebar, top navigation, and shell used across the app.
- `components/providers` contains the theme provider used for light and dark mode.
