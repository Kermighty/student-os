# Prisma

This directory contains the database schema and the data layer configuration for Student OS.

The schema defines the core user and Auth.js models used by credentials authentication and future student records.
