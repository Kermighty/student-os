# Types

This directory is reserved for shared TypeScript contracts and domain types as the application grows.

The foundation keeps type definitions centralized so future feature modules can stay strongly typed and maintainable.
