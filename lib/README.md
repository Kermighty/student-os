# Shared libraries

This folder contains infrastructure logic shared across the application.

- `prisma.ts` centralizes the Prisma client instance.
- `utils.ts` holds small shared helper functions such as class merging logic.
